# Discord application (client) id — required when running inside Discord.
VITE_DISCORD_CLIENT_ID=

# Set to "true" to skip the Discord SDK handshake (local dev / E2E outside Discord).
VITE_DEV_BYPASS=true
