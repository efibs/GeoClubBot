# The Discord application (client) id is NOT set here — the frontend fetches it from the backend at
# runtime (the API's DiscordActivity:ClientId), so the bundle isn't tied to one Discord application.

# Set to "true" to skip the Discord SDK handshake (local dev / E2E outside Discord).
VITE_DEV_BYPASS=true
