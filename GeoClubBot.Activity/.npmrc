# @tanstack/vue-query declares an *optional* peer on @vue/composition-api, which itself only
# supports Vue 2. On our Vue 3 project npm 10 still tries (and fails) to resolve that optional
# peer, so relax strict peer resolution. Applies to `npm ci` in CI too, keeping installs identical.
legacy-peer-deps=true
